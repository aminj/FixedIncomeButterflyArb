function c=SwapRate2Zero(ThisSwapRates, ZeroMaturity)
%ThisSwapRates should be in the fomr of @(mat) ThisSwapRate(thisdate, mat)
max_mat=ZeroMaturity;
swap_freq=.25;
T=swap_freq:swap_freq:max_mat;
B=zeros(T);
count=0;
for i=T
    count=count+1;
    if count==1
        B(count)=1/(1+ThisSwapRates(i));
    else
        Ci=ThisSwapRates(i);
        BC=sum(B*Ci);
        B(count)=(1
        

    
    
